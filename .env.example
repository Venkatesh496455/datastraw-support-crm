# Free Hugging Face token, used for AI-assisted priority suggestions.
# Get one at https://huggingface.co/settings/tokens (read access is enough).
# App works fine without it - priority suggestion just defaults to "Normal".
HF_API_TOKEN=

PORT=8000
